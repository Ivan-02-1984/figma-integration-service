# Figma Integration Service

Простой сервис для экспорта данных и изображений из Figma в JSON и ZIP архив.  
Сервис запускается как самостоятельное приложение (exe или bat) и открывает форму в браузере для работы без IDE и установки Java.

---

## 🚀 Как запустить

1. Скачайте готовый файл `figma-integration-service.exe` (или `.bat`).
2. Дважды кликните по файлу.
3. Откроется браузер с формой для ввода данных: токена и ID Figma файла.

---

## 🔑 Token

Это **секретный ключ**, который позволяет сервису получать доступ к вашим Figma-файлам.

**Как получить токен:**

1. Войдите в [Figma](https://www.figma.com) и откройте **Account Settings** через аватар в правом верхнем углу.
2. Перейдите в раздел **Security**.
3. В блоке **Personal Access Tokens** нажмите **Generate new token** (Сгенерировать новый токен).
4. Дайте токену осмысленное имя, например: `Figma Export Service`.
5. Выберите срок действия токена (можно оставить **No expiration**, чтобы токен не истекал).
6. Нажмите **Generate Token**.
7. Скопируйте токен **и сохраните его сразу**, так как после закрытия окна его уже нельзя будет посмотреть. Если вы потеряете ключ, придётся создавать новый, повторив шаги 3–6.
8. Вставьте токен в поле **Token** на форме сервиса.

**Дополнительно:**

- Старые или неиспользуемые токены можно удалить в разделе Security.
- Не удаляйте токен, который используется в сервисе.
- Никому не передавайте токен — он даёт полный доступ к вашим файлам.

---

## 📝 File ID

Это уникальный идентификатор вашего Figma файла:

1. Откройте файл в Figma.
2. Скопируйте часть URL после `/file/`, до следующего слэша или параметров.
    - Пример: `https://www.figma.com/file/ABC123XYZ/Project-Name` → **File ID = ABC123XYZ**
3. Вставьте File ID в поле **File ID** на форме сервиса.

---

## 🖥 Форма сервиса

Форма в браузере содержит два поля и две кнопки:

| Поле / Кнопка        | Назначение |
|----------------------|------------|
| **Token**            | Вставьте сгенерированный Personal Access Token из Figma (см. выше). |
| **File ID**           | Вставьте идентификатор файла Figma. |
| **Скачать JSON**      | Экспортирует структуру файла в JSON и автоматически начнётся скачивание файла `figma.json`. |
| **Скачать ZIP**       | Экспортирует весь файл и все картинки в ZIP архив (`figma-archive.zip`) с папками по структуре Page → Frame → Node. |

---

## 📦 Что вы получаете

- **JSON** — структура файла Figma (страницы, фреймы, тексты, изображения).
- **ZIP архив** — все изображения и `figma.json`, структурированные по папкам Page/Frame/Node.

---

## ⚠ Ограничения

- Сервис не требует установки Java или IDE.
- Если Figma вернёт ошибку лимита, JSON и ZIP будут недоступны до сброса лимита.
- Для файлов, где у вас нет прав на экспорт, изображения не будут загружены, но JSON будет доступен.  
